{
  "name": "@kaggle-lint/core",
  "version": "1.0.1",
  "description": "Core linting logic for Kaggle Linter",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run copy-pyodide",
    "copy-pyodide": "copyfiles -u 2 \"src/pyodide/**/*\" dist/pyodide",
    "dev": "tsc --watch",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "linter",
    "python",
    "kaggle"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "copyfiles": "^2.4.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.6"
  }
}
